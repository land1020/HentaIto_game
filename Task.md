# タスク: 結果発表画面のスコア表示変更

ユーザーの要望に基づき、結果発表画面（ラウンド結果）のスコア表示仕様を変更します。

## 要望
1. **メインスコアの変更**: 1位～3位に表示する点数を「特別賞」と「順位点数」を加算した数値にする。
2. **累計スコア非表示**: 最終結果までドキドキ感を維持するため、累計スコアを非表示にする。

## 調査と設計
- [x] `ResultScreen.tsx` と `RankingList.tsx` の実装を確認 <!-- id: 4 -->
- [x] スコアデータ（`players` vs `roundResults`）の使われ方を確認 <!-- id: 5 -->
- [x] ボーナス点（特別賞、順位点）がどこで計算・保持されているか確認し、合計値の算出方法を決定 <!-- id: 6 -->

## 実装
- [x] `App.tsx` のスコア計算ロジック (`calculateTotalScore`) 修正
    - 「ラウンド素点 + 特別賞」で一時的にランク付けを行う
    - そのランクに基づいて順位ボーナスを加算する
    - 最終的なラウンド合計スコアを算出する
- [x] `RankingList.tsx` の修正
    - `roundResults` がある場合、算出された「ラウンド合計スコア」でソートして表示
    - 累計スコアの非表示化


## 確認
- [ ] 結果発表画面で、上位プレイヤーのスコアがボーナス込みの値になっているか確認
- [ ] 累計スコアが表示されていないことを確認
